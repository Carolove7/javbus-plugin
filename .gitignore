# 合并索引分片由 build_data.js 在构建期生成（不进 git；函数运行时从 jsDelivr 拉取 index/ 分片合并，已验证可用）
cloud-functions/_data/
# 构建期写入的部署 commit SHA（供 search.js 固定 jsDelivr 数据源，避免 @master CDN 滞后）；不入库，避免陈旧 SHA
cloud-functions/_ref.json
node_modules/
# EdgeOne Makers 部署产物 / 本地环境（非源码；远端构建会自行重建 .edgeone）
.edgeone/
.env
package-lock.json
# WorkBuddy 本地工作区数据（含项目笔记/部署信息，不入库）
.workbuddy/
cloud-functions/_ref.js
# Git 连接部署（makers-wfh5gmrpw0pi）的构建产物：build_pages.mjs 在远端把可部署文件
# 拷贝进 dist/ 作为 OutputDir；dist/ 由远端构建生成，不入库（避免陈旧副本 + 与本地 CLI 部署区分）
dist/
